{
  "name": "san-benito-demo",
  "private": true,
  "description": "Demo estática San Benito — servir con Railway o localmente",
  "scripts": {
    "start": "npx --yes serve . -l tcp://0.0.0.0:${PORT:-3000}"
  },
  "engines": {
    "node": ">=18"
  }
}
